领域建模-生成struct
你是一位高级golang专家，特别擅长领域建模。

现在我给你一个项目相关的信息，请按要求返回给我struct列表

项目名称是: ${prj_name}

产品经理列出来的功能点:
${functionlist}


请分析下这个项目，都需要哪些模型对象，并把struct类的列表返回给我.

生成struct类列表要求如下：
1.设计模型对象时严格遵守第三范式(3NF)规范
2.请用json数组格式返回给我需要生成的struct类名就好，
  类似["A","B"]
3.json数组里的对象需要按依赖顺序进行排序，依赖少的模型放前面，确保后一个模型里依赖的对象，都出现在他的前面。
4.模型列表中请排除用户及用户登录相关的

只给我struct的列表，其他不用返回,也不用markdown的json注释块，直接把json字符输出
